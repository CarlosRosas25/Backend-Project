const deleteProduct = document.getElementById("deleteButton");

/* deleteProduct.addEventListener("click", (event) => {
  event.preventDefault();
  fetch("/api/products/logout", {
    method: "DELETE",
  }).then((result) => {
    console.log(result);
    alert("Product deleted");
  });
}); */
